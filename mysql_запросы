CREATE TABLE User (
    Login   VARCHAR(30),
    Pass    VARCHAR(130)
);

CREATE TABLE Category (
    Name   VARCHAR(30)
);

CREATE TABLE Item (
    Name    VARCHAR(30),
    Price   INT
);

CREATE TABLE Item_Cat (
    ItemName    VARCHAR(30),
    CatName    VARCHAR(30)
);

CREATE TABLE Basket (
    LoginName    VARCHAR(30),
    ItemName        VARCHAR(30),
    Count   INT,
    Buy INT
);


============================
CREATE TABLE User (
    Login   VARCHAR(30) PRIMARY KEY,
    Pass    VARCHAR(130)
);

CREATE TABLE Category (
    Name   VARCHAR(30) PRIMARY KEY
);

CREATE TABLE Item (
    Name    VARCHAR(30) PRIMARY KEY,
    Price   INT
);

CREATE TABLE Item_Cat (
    ItemName    VARCHAR(30),
    FOREIGN KEY (ItemName)  REFERENCES Item (Name),
    CatName    VARCHAR(30),
    FOREIGN KEY (CatName)  REFERENCES Category (Name)
);

CREATE TABLE Basket (
    LoginName    VARCHAR(30),
    FOREIGN KEY (LoginName)  REFERENCES User (Login),
    ItemName        VARCHAR(30),
    FOREIGN KEY (ItemName)  REFERENCES Item (Name),
    Count   INT,
    Buy INT
);

//ALTER TABLE Item ADD PRIMARY KEY (Name);


INSERT INTO user VALUES(100, 'Thomas', 'Sales', 5000);